NEP_greater_nepal_r56 = {
	#NEP_integrate_bihar_r56 = {
	#	icon = GFX_decision_concessions
	#	fire_only_once = yes
	#	days_remove = 100
	#	priority = 40
#
	#	available = {
	#		1092 = {
	#			is_controlled_by_ROOT_or_subject = yes
	#			compliance > 50
	#			resistance < 5
	#		}
	#	}
#
	#	visible = { 1092 = { NOT = { is_core_of = ROOT } } }
	#	highlight_states = { highlight_state_targets = { state = 1092 } }
	#	cancel_trigger = { 1092 = { is_controlled_by_ROOT_or_subject = no }  }
	#	ai_will_do = { base = 5 }
#
	#	modifier = {
	#		political_power_factor = -0.25
	#		consumer_goods_factor = 0.1
	#	}
#
	#	complete_effect = { add_manpower = -10000 }
#
	#	remove_effect = {
	#		add_political_power = 50
	#		add_stability = 0.07
	#		1092 = {
	#			add_core_of = ROOT
	#			if = {
	#				limit = { 1092 = { is_Claimed_by = ROOT } }
	#				remove_claim_by = ROOT
	#			}
	#			if = {
	#				limit = { 1092 = { NOT = { is_owned_by = ROOT } } }
	#				transfer_state_to = ROOT
	#			}
	#		}
	#	}
	#}

	NEP_integrate_uttarakhand_r56 = {
		icon = GFX_decision_CHL_mapuche_reorganize_the_brazilian_government
		fire_only_once = yes
		days_remove = 60
		priority = 30

		available = {
			1090 = {
				is_controlled_by_ROOT_or_subject = yes
				compliance > 35
				resistance < 5
			}
		}

		visible = { 1090 = { NOT = { is_core_of = ROOT } } }
		highlight_states = { highlight_state_targets = { state = 1090 } }
		cancel_trigger = { 1090 = { is_controlled_by_ROOT_or_subject = no }  }
		ai_will_do = { base = 5 }

		modifier = {
			political_power_factor = -0.15
			consumer_goods_factor = 0.05
		}

		complete_effect = { add_manpower = -2500 }

		remove_effect = {
			add_political_power = 35
			add_stability = 0.04
			1090 = {
				add_core_of = ROOT
				if = {
					limit = { 1090 = { is_Claimed_by = ROOT } }
					remove_claim_by = ROOT
				}
				if = {
					limit = { 1090 = { NOT = { is_owned_by = ROOT } } }
					transfer_state_to = ROOT
				}
			}
		}
	}

	NEP_integrate_sikkim_r56 = {
		icon = GFX_decision_CHL_mapuche_liberate_inuit
		fire_only_once = yes
		days_remove = 30
		priority = 20

		available = {
			1013 = { 
				is_controlled_by_ROOT_or_subject = yes
				compliance > 20
				resistance < 5
			}
		}

		visible = { 1013 = { NOT = { is_core_of = ROOT } } }
		highlight_states = { highlight_state_targets = { state = 1013 } }
		cancel_trigger = { 1013 = { is_controlled_by_ROOT_or_subject = no }  }
		ai_will_do = { base = 5 }

		modifier = { political_power_factor = -0.05 }

		remove_effect = {
			add_political_power = 20
			add_stability = 0.02
			1013 = {
				add_core_of = ROOT
				if = {
					limit = { 1013 = { is_Claimed_by = ROOT } }
					remove_claim_by = ROOT
				}
				if = {
					limit = { 1013 = { NOT = { is_owned_by = ROOT } } }
					transfer_state_to = ROOT
				}
			}
		}
	}
}

NEP_foreign_relations_r56 = {
	# Join the Allies
	NEP_join_allies_r56 = {
		icon = GFX_decision_eng_trade_unions_support
		fire_only_once = yes
		targets = { ENG }
		priority = 25

		available = {
			is_in_faction = no
			is_subject = no
			NOT = { has_war_with = ENG }
			ENG = { 
				has_capitulated = no
				OR =  {
					has_government = neutrality
					has_government = democratic
				}
			}
		}

		visible = {
			has_completed_focus = NEP_maintain_british_ties_r56
			ENG = {
				is_faction_leader = yes
				NOT = { is_in_faction_with = ROOT }
			}
		}

		ai_will_do = {
			base = 0 #so that u only join through event
			modifier = { #hmmmm okie dokie!
				add = 1
				has_government = democratic 
			}
		}

		complete_effect = { ENG = { country_event = generic.2 } }
	}

	# Joint British-Nepali Military Exercises
	NEP_joint_british_nepalese_military_exercises_r56 = {
		icon = GFX_decision_generic_army_support
		fire_only_once = no
		targets = { ENG }
		priority = 20

		days_re_enable = 100

		custom_cost_text = EGY_decision_cost_army_experience
		custom_cost_trigger = { has_army_experience > 9 }

		available = {
			ENG = {
				NOT = { has_war_with = ROOT }
				exists = yes
			}
		}

		visible = { has_completed_focus = NEP_maintain_british_ties_r56 }

		ai_will_do = { base = 0 } # dont be annoying
		complete_effect = { 
			ENG = { country_event = nepal.4 }
			custom_effect_tooltip = if_they_accept_tt
			effect_tooltip = {
				add_timed_idea = {
					idea = NEP_joint_military_exercise_r56
					days = 100
				}
			}
		}
	}

	# Purchase British Weapons
	NEP_purchase_british_weapons_r56 = {
		icon = GFX_decision_generic_prepare_civil_war
		fire_only_once = no
		targets = { ENG }
		priority = 15

		cost = 25
		days_re_enable = 100
		days_remove = 50

		available = {
			ENG = {
				NOT = { has_war_with = ROOT }
				exists = yes
			}
		}

		visible = { has_completed_focus = NEP_maintain_british_ties_r56 }
		ai_will_do = { base = 1 }

		modifier = { consumer_goods_factor = 0.08 }

		complete_effect = {
			add_equipment_to_stockpile = {
				type = infantry_equipment
				amount = 1500
				producer = ENG
			}
			custom_effect_tooltip = generic_skip_one_line_tt
			add_equipment_to_stockpile = {
				type = artillery_equipment
				amount = 50
				producer = ENG
			}
		}
	}

	# Aqcuire British Ships
	NEP_acquire_british_ships_r56 = {
		icon = GFX_decision_generic_merge_plant_ship
		fire_only_once = yes
		targets = { ENG }
		priority = 10

		cost = 30

		available = {
			any_controlled_state = { is_coastal = yes }
			ENG = {
				exists = yes
				NOT = { has_war_with = ROOT }
				any_controlled_state = { is_coastal = yes }
			}
		}

		visible = { 
			has_completed_focus = NEP_maintain_british_ties_r56 
			any_controlled_state = { is_coastal = yes }
		}

		ai_will_do = { base = 5 }

		complete_effect = {
			ENG = { country_event = nepal.16 }
			custom_effect_tooltip = if_they_accept_tt
			effect_tooltip = {
				add_equipment_to_stockpile = {
					type = convoy
					amount = 50
				}
				custom_effect_tooltip = generic_skip_one_line_tt
				add_offsite_building = { type = dockyard level = 1 }
				add_tech_bonus = {
					name = NEP_learnings_from_british_ships_r56
					bonus = 0.5
					uses = 1
					category = naval_doctrine
				}
				custom_effect_tooltip = generic_skip_one_line_tt
				add_opinion_modifier = {
					target = ROOT
					modifier = large_increase
				}
			}
		}
	}

	# Invite American Economic Advisors
	NEP_invite_american_economic_advisors_r56 = {
		icon = GFX_decision_gre_investment_decisions
		fire_only_once = no
		targets = { USA }
		priority = 8

		cost = 40
		days_re_enable = 100
		days_remove = 80

		available = { NOT = { has_war_with = USA } }
		visible = { has_completed_focus = NEP_normalize_relations_with_the_united_states_r56 }
		ai_will_do = { base = 3 }

		modifier = {
			industrial_capacity_factory = 0.08
			production_speed_industrial_complex_factor = 0.1
			democratic_drift = 0.03
		}

		remove_effect = {
			add_tech_bonus = {
				name = NEP_learnings_from_american_advisors_r56
				bonus = 0.35
				uses = 1
				category = industry
			}
		}
	}

	# Purchase American Motorized Equipment
	NEP_purchase_american_motorized_equipment_r56 = {
		icon = GFX_decision_railroads
		fire_only_once = no
		targets = { USA }
		priority = 15

		cost = 30
		days_re_enable = 120
		days_remove = 50

		available = {
			has_tech = tech_trucks
			USA = {
				NOT = { has_war_with = ROOT }
				exists = yes
			}
		}

		visible = { has_completed_focus = NEP_normalize_relations_with_the_united_states_r56 }
		ai_will_do = { base = 1 }

		modifier = { consumer_goods_factor = 0.1 }

		complete_effect = {
			add_equipment_to_stockpile = {
				type = motorized_equipment
				amount = 100
				producer = USA
			}
		}
	}

	# Request Border Revision with India 
	NEP_request_british_border_revisions_r56 = { #todo
		icon = GFX_decision_generic_nationalism
		fire_only_once = yes
		priority = 25

		available = {
			NOT = { OR = { has_war_with = ENG has_War_with = RAJ } }
		}

		visible = {
			OR = {
				has_completed_focus = NEP_renegotiate_unfair_treaties_r56
				has_completed_focus = NEP_address_the_new_india_r56
			}
			NOT = {
				controls_state = 1090
				controls_state = 1013
				controls_state = 1092
			}
		}

		ai_will_do = { base = 0 }

		complete_effect = {
			
		}
	}

	# Break the British Monopoly
	NEP_break_the_british_monopoly_r56 = { #todo
		icon = GFX_decision_ger_military_buildup
		fire_only_once = yes
		targets = { ENG }
		priority = 25

		available = {
			country_exists = ENG
			NOT = { is_subject_of = ENG }
		}

		visible = {
			has_completed_focus = NEP_renegotiate_unfair_treaties_r56
			has_idea = NEP_market_restriction_r56
		}

		ai_will_do = { base = 0 }

		complete_effect = {
			
		}
	}

	# Bypass the Strict Market Restrictions
	NEP_cheat_on_britain_r56 = {
		icon = GFX_decision_ger_military_buildup
		fire_only_once = yes
		priority = 25
		cost = 50

		visible = {
			OR = {
				has_completed_focus = NEP_recognize_the_soviet_regime_r56
				has_completed_focus = NEP_approach_japan_r56
			}
			has_idea = NEP_market_restriction_r56
		}

		ai_will_do = { base = 5 }

		complete_effect = {
			remove_ideas = NEP_market_restriction_r56
			custom_effect_tooltip = generic_skip_one_line_tt
			ENG = {
				country_event = nepal.19
				add_opinion_modifier = {
					target = ROOT
					modifier = large_decrease
				}
			}
		}
	}

	##########################################################################

	# Join the Comintern
	NEP_join_comintern_r56 = {
		icon = GFX_decision_eng_trade_unions_support
		fire_only_once = yes
		targets = { SOV }
		priority = 25

		available = {
			has_government = communism
			is_in_faction = no
			is_subject = no
			NOT = { has_war_with = SOV }
			SOV = { 
				has_capitulated = no
				has_government = communism
			}
		}

		visible = {
			has_completed_focus = NEP_recognize_the_soviet_regime_r56
			SOV = {
				is_faction_leader = yes
				NOT = { is_in_faction_with = ROOT }
			}
		}

		ai_will_do = {
			base = 1
			modifier = {
				add = 1
				has_government = communism 
			}
		}

		complete_effect = { SOV = { country_event = generic.2 } }
	}

	# Purchase Soviet Weapons
	NEP_purchase_soviet_weapons_r56 = {
		icon = GFX_decision_generic_prepare_civil_war
		fire_only_once = no
		targets = { SOV }
		priority = 15

		cost = 25
		days_re_enable = 100
		days_remove = 50

		available = {
			SOV = {
				NOT = { has_war_with = ROOT }
				exists = yes
			}
		}

		visible = { has_completed_focus = NEP_recognize_the_soviet_regime_r56 }
		ai_will_do = { base = 1 }

		modifier = { consumer_goods_factor = 0.08 }

		complete_effect = {
			add_equipment_to_stockpile = {
				type = infantry_equipment
				amount = 1500
				producer = SOV
			}
			custom_effect_tooltip = generic_skip_one_line_tt
			add_equipment_to_stockpile = {
				type = artillery_equipment
				amount = 50
				producer = SOV
			}
		}
	}

	# Invite Soviet Economic Advisors
	NEP_invite_soviet_economic_advisors_decision_r56 = {
		name = NEP_invite_soviet_economic_advisors_r56
		icon = GFX_decision_gre_investment_decisions
		fire_only_once = no
		targets = { SOV }
		priority = 20

		cost = 40
		days_re_enable = 100
		days_remove = 80

		available = { NOT = { has_war_with = SOV } }
		visible = { has_completed_focus = NEP_invite_soviet_economic_advisors_r56 }
		ai_will_do = { base = 3 }

		modifier = {
			industrial_capacity_factory = 0.08
			production_speed_arms_factory_factor = 0.1
			communism_drift = 0.03
		}

		remove_effect = {
			add_tech_bonus = {
				name = NEP_learnings_from_soviet_advisors_r56
				bonus = 0.35
				uses = 1
				category = industry
			}
		}
	}

	# Promote Worker's Culture
	NEP_promote_workers_culture_r56 = {
		icon = GFX_decision_generic_industry
		fire_only_once = no
		priority = 20

		cost = 40
		days_re_enable = 150
		days_remove = 50

		visible = { has_completed_focus = NEP_invite_soviet_economic_advisors_r56 }
		ai_will_do = { base = 3 }

		modifier = {
			industrial_capacity_factory = 0.1
			industrial_capacity_dockyard = 0.1
			communism_drift = 0.05
		}
	}

	# Invite Soviet NKVD
	NEP_invite_nkvd_r56 = {
		icon = GFX_decision_SWI_expand_covert_operations
		fire_only_once = yes
		priority = 20

		cost = 50
		days_remove = 25

		available = { NOT = { has_war_with = SOV } }
		visible = { has_completed_focus = NEP_recognize_the_soviet_regime_r56 }
		ai_will_do = { base = 5 }

		modifier = { political_power_factor = -0.15 communism_drift = 0.03 }

		remove_effect = {
			if = {
				limit = {
					has_dlc = "La Resistance"
					NOT = { has_intelligence_agency = yes }
				}
				create_intelligence_agency = yes
				custom_effect_tooltip = generic_skip_one_line_tt			
			}

			add_ideas = NEP_nkvd_r56
			custom_effect_tooltip = generic_skip_one_line_tt
			add_tech_bonus = {
				name = mp_tech_bonus_name
				bonus = 1
				uses = 1
				category = military_police_tech
			}
		}
	}

	# Purchase Soviet Light Tanks
	NEP_purchase_soviet_light_tanks_r56 = {
		icon = GFX_decision_generic_merge_plant_tank
		fire_only_once = no
		targets = { SOV }
		priority = 15

		cost = 35
		days_re_enable = 150
		days_remove = 50

		available = {
			OR = {
				has_tech = basic_light_tank 
				has_tech = basic_light_tank_chassis
			}
			SOV = {
				NOT = { has_war_with = ROOT }
				exists = yes
			}
		}

		visible = { has_completed_focus = NEP_order_soviet_heavy_weapons_r56 }
		ai_will_do = { base = 1 }

		modifier = { consumer_goods_factor = 0.1 }

		complete_effect = {
			add_equipment_to_stockpile = {
				type = light_tank_chassis
				producer = SOV
				amount = 75
			}
		}
	}

	# Purchase Soviet Medium Tanks
	NEP_purchase_soviet_medium_tanks_r56 = {
		icon = GFX_decision_generic_merge_plant_tank
		fire_only_once = no
		targets = { SOV }
		priority = 20

		cost = 50
		days_re_enable = 170
		days_remove = 50

		available = {
			OR = {
				has_tech = basic_medium_tank
				has_tech = improved_medium_tank #needed because basic can be bypassed
				has_tech = basic_medium_tank_chassis
				has_tech = improved_medium_tank_chassis
			}
			SOV = {
				NOT = { has_war_with = ROOT }
				exists = yes
				OR = {
					has_tech = basic_medium_tank
					has_tech = improved_medium_tank #needed because basic can be bypassed
					has_tech = basic_medium_tank_chassis
					has_tech = improved_medium_tank_chassis
				}
			}
		}

		visible = { has_completed_focus = NEP_order_soviet_heavy_weapons_r56 }
		ai_will_do = { base = 0 } # dont blow ur eco

		modifier = { consumer_goods_factor = 0.15 }

		complete_effect = {
			add_equipment_to_stockpile = {
				type = medium_tank_chassis
				producer = SOV
				amount = 50
			}
		}
	}

	# Join CCP Faction
	NEP_join_ccp_faction_r56 = {
		icon = GFX_decision_eng_trade_unions_support
		fire_only_once = yes
		targets = { PRC }
		priority = 25

		available = {
			has_government = communism
			is_in_faction = no
			is_subject = no
			NOT = { has_war_with = PRC }
			PRC = { 
				has_capitulated = no
				has_government = communism
			}
		}

		visible = {
			has_completed_focus = NEP_support_ccp_r56
			PRC = {
				is_faction_leader = yes
				NOT = { is_in_faction_with = ROOT }
			}
		}

		ai_will_do = {
			base = 1
			modifier = {
				add = 1
				has_government = communism 
			}
		}

		complete_effect = { PRC = { country_event = generic.2 } }
	}

	# Send Weapons to Communist China
	NEP_send_weapons_to_ccp_r56 = {
		icon = GFX_decision_generic_prepare_civil_war
		fire_only_once = no
		targets = { PRC }
		priority = 15

		cost = 25
		days_re_enable = 100

		available = {
			PRC = {
				NOT = { has_war_with = ROOT }
				has_government = communism
				exists = yes
			}

			has_equipment = { infantry_equipment > 1999 }
			has_equipment = { support_equipment > 24 }
			has_equipment = { artillery_equipment > 49 }
		}

		visible = { has_completed_focus = NEP_support_ccp_r56 }
		ai_will_do = { base = 1 }

		complete_effect = {
			effect_tooltip = {
				send_equipment = {
					type = infantry_equipment
					amount = 2000
					target = PRC
				}
				custom_effect_tooltip = generic_skip_one_line_tt
				send_equipment = {
					type = support_equipment
					amount = 25
					target = PRC
				}
				custom_effect_tooltip = generic_skip_one_line_tt
				send_equipment = {
					type = artillery_equipment
					amount = 50
					target = PRC
				}
			}
			#event to prc
		}
	}

	# Joint British-Nepali Military Exercises
	NEP_joint_pla_nepalese_military_exercises_r56 = {
		icon = GFX_decision_generic_army_support
		fire_only_once = no
		targets = { PRC }
		priority = 20

		days_re_enable = 100

		custom_cost_text = EGY_decision_cost_army_experience
		custom_cost_trigger = { has_army_experience > 9 }

		available = {
			PRC = {
				NOT = { has_war_with = ROOT }
				exists = yes
			}
		}

		visible = { has_completed_focus = NEP_support_ccp_r56 }

		ai_will_do = { base = 0 } # dont be annoying
		complete_effect = { 
			PRC = { country_event = nepal.4 }
			custom_effect_tooltip = if_they_accept_tt
			effect_tooltip = {
				add_timed_idea = {
					idea = NEP_joint_military_exercise_r56
					days = 100
				}
			}
		}
	}

	##########################################################################

	# Join the Great East Asian Co-prosperity Sphere
	NEP_join_geacps_r56 = {
		icon = GFX_decision_eng_trade_unions_support
		fire_only_once = yes
		targets = { JAP }
		priority = 25

		available = {
			is_in_faction = no
			is_subject = no
			NOT = { has_war_with = JAP }
			JAP = { 
				has_capitulated = no
				OR = {
					has_government = neutrality
					has_government = fascism
				}
			}
		}

		visible = {
			has_completed_focus = NEP_approach_japan_r56
			JAP = {
				is_faction_leader = yes
				NOT = { is_in_faction_with = ROOT }
			}
		}

		ai_will_do = {
			base = 1
			modifier = {
				add = 1
				has_government = fascism 
			}
		}

		complete_effect = { JAP = { country_event = generic.2 } }
	}

	# Purchase Japanese Weapons
	NEP_purchase_japanese_weapons_r56 = {
		icon = GFX_decision_generic_prepare_civil_war
		fire_only_once = no
		targets = { JAP }
		priority = 15

		cost = 25
		days_re_enable = 100
		days_remove = 50

		available = {
			JAP = {
				NOT = { has_war_with = ROOT }
				exists = yes
			}
		}

		visible = { has_completed_focus = NEP_approach_japan_r56 }
		ai_will_do = { base = 1 }

		modifier = { consumer_goods_factor = 0.08 }

		complete_effect = {
			add_equipment_to_stockpile = {
				type = infantry_equipment
				amount = 1500
				producer = JAP
			}
			custom_effect_tooltip = generic_skip_one_line_tt
			add_equipment_to_stockpile = {
				type = artillery_equipment
				amount = 50
				producer = JAP
			}
		}
	}

	##########################################################################

	NEP_volunteers_in_the_sinojapanese_war_r56 = {
		icon = GFX_decision_generic_reorganize_irregulars
		fire_only_once = yes
		priority = 10

		targets = { CUG }

		available = {
			has_war = no
			CUG = { has_war = yes has_capitulated = no }
		}

		visible = {
			has_completed_focus = NEP_strengthen_chinese_ties_r56
			has_completed_focus = NEP_the_nepali_expeditionary_force_r56
			NOT = { has_war_with = CUG }
		}

		complete_effect = { add_ideas = NEP_involvement_in_the_sinojapanese_war_r56 }
	}
}

NEP_transhimalayan_politics_r56 = {
	NEP_invite_tibet_into_defensive_pact_r56 = { # This exists incase PREV isnt invited previously
		icon = GFX_decision_eng_trade_unions_support
		targets = { TIB }
		fire_only_once = yes
		priority = 25

		available = {
			has_war = no
			is_subject = no
			if = {
				limit = { is_faction_leader = yes }
				TIB = {
					exists = yes
					is_subject = no
					has_war = no
					is_in_faction = no
					if = { # So that we don't invite enemy ideology
						limit = { ROOT = { has_government = communism } }
						OR = {
							has_government = communism
							has_government = neutrality
						}

						else_if = {
							limit = { ROOT = { has_government = democratic } }
							OR = {
								has_government = democratic
								has_government = neutrality
							}
						}

						else_if = {
							limit = { ROOT = { has_government = fascism } }
							OR = {
								has_government = fascism
								has_government = neutrality
							}
						}
					}
				}
				else = {
					TIB = {
						exists = yes
						is_subject = no
						has_war = no
					}
					if = { # So that we don't invite enemy ideology
						limit = { ROOT = { has_government = communism } }
						OR = {
							has_government = communism
							has_government = neutrality
						}

						else_if = {
							limit = { ROOT = { has_government = democratic } }
							OR = {
								has_government = democratic
								has_government = neutrality
							}
						}

						else_if = {
							limit = { ROOT = { has_government = fascism } }
							OR = {
								has_government = fascism
								has_government = neutrality
							}
						}
					}
				}
			}
		}

		visible = {
			has_country_flag = NEP_we_want_friendship # So that this doesn't appear if Nepal decides to go expansionist
			TIB = {
				exists = yes
				NOT = {
					is_subject_of = ROOT
					is_in_faction_with = ROOT
					OR = {
						has_country_flag = NEP_we_dont_want_alliance_with_you_nepalese # If they are independent at game start then they get invited already. Put this here to prevent this decision from popping up if they already declined / accepted
						has_country_flag = NEP_accepted_himalayan_defense_pact # If they are independent at game start then they get invited already. Put this here to prevent this decision from popping up if they already declined / accepted
					}
				}
			}
		}

		ai_will_do = { base = 1 }

		complete_effect = {
			TIB = {
				country_event = {
					id = nepal.8
					days = 2
					hours = 12
				}
			}
			custom_effect_tooltip = generic_skip_one_line_tt
			custom_effect_tooltip = NEP_they_are_more_likely_to_accept_if
		}
	}

	NEP_invite_bhutan_into_defensive_pact_r56 = { # This exists incase PREV isnt invited previously
		icon = GFX_decision_eng_trade_unions_support
		targets = { BHU }
		fire_only_once = yes
		priority = 25

		available = {
			has_war = no
			is_subject = no
			OR = { # We don't share borders, duh
				TIB = { has_country_flag = NEP_accepted_himalayan_defense_pact }
				SKK = { has_country_flag = NEP_accepted_himalayan_defense_pact }
			}
			if = {
				limit = { is_faction_leader = yes }
				BHU = {
					exists = yes
					is_subject = no
					has_war = no
					is_in_faction = no
					if = { # So that we don't invite enemy ideology
						limit = { ROOT = { has_government = communism } }
						OR = {
							has_government = communism
							has_government = neutrality
						}

						else_if = {
							limit = { ROOT = { has_government = democratic } }
							OR = {
								has_government = democratic
								has_government = neutrality
							}
						}

						else_if = {
							limit = { ROOT = { has_government = fascism } }
							OR = {
								has_government = fascism
								has_government = neutrality
							}
						}
					}
				}
				else = {
					BHU = {
						exists = yes
						is_subject = no
						has_war = no
					}
					if = { # So that we don't invite enemy ideology
						limit = { ROOT = { has_government = communism } }
						OR = {
							has_government = communism
							has_government = neutrality
						}

						else_if = {
							limit = { ROOT = { has_government = democratic } }
							OR = {
								has_government = democratic
								has_government = neutrality
							}
						}

						else_if = {
							limit = { ROOT = { has_government = fascism } }
							OR = {
								has_government = fascism
								has_government = neutrality
							}
						}
					}
				}
			}
		}

		visible = {
			has_country_flag = NEP_we_want_friendship # So that this doesn't appear if Nepal decides to go expansionist
			BHU = {
				exists = yes
				NOT = {
					is_subject_of = ROOT
					is_in_faction_with = ROOT
					OR = {
						has_country_flag = NEP_we_dont_want_alliance_with_you_nepalese # If they are independent at game start then they get invited already. Put this here to prevent this decision from popping up if they already declined / accepted
						has_country_flag = NEP_accepted_himalayan_defense_pact # If they are independent at game start then they get invited already. Put this here to prevent this decision from popping up if they already declined / accepted
					}
				}
			}
		}

		ai_will_do = { base = 1 }

		complete_effect = {
			BHU = {
				country_event = {
					id = nepal.8
					days = 2
					hours = 12
				}
			}
			custom_effect_tooltip = generic_skip_one_line_tt
			custom_effect_tooltip = NEP_they_are_more_likely_to_accept_if
		}
	}
	
	NEP_invite_sikkim_into_defensive_pact_r56 = { # This exists incase PREV isnt invited previously
		icon = GFX_decision_eng_trade_unions_support
		targets = { SKK }
		fire_only_once = yes
		priority = 25

		available = {
			has_war = no
			is_subject = no
			if = {
				limit = { is_faction_leader = yes }
				SKK = {
					exists = yes
					is_subject = no
					has_war = no
					is_in_faction = no
					if = { # So that we don't invite enemy ideology
						limit = { ROOT = { has_government = communism } }
						OR = {
							has_government = communism
							has_government = neutrality
						}

						else_if = {
							limit = { ROOT = { has_government = democratic } }
							OR = {
								has_government = democratic
								has_government = neutrality
							}
						}

						else_if = {
							limit = { ROOT = { has_government = fascism } }
							OR = {
								has_government = fascism
								has_government = neutrality
							}
						}
					}
				}
				else = {
					SKK = {
						exists = yes
						is_subject = no
						has_war = no
					}
					if = { # So that we don't invite enemy ideology
						limit = { ROOT = { has_government = communism } }
						OR = {
							has_government = communism
							has_government = neutrality
						}

						else_if = {
							limit = { ROOT = { has_government = democratic } }
							OR = {
								has_government = democratic
								has_government = neutrality
							}
						}

						else_if = {
							limit = { ROOT = { has_government = fascism } }
							OR = {
								has_government = fascism
								has_government = neutrality
							}
						}
					}
				}
			}
		}

		visible = {
			has_country_flag = NEP_we_want_friendship # So that this doesn't appear if Nepal decides to go expansionist
			SKK = {
				exists = yes
				NOT = {
					is_subject_of = ROOT
					is_in_faction_with = ROOT
					OR = {
						has_country_flag = NEP_we_dont_want_alliance_with_you_nepalese # If they are independent at game start then they get invited already. Put this here to prevent this decision from popping up if they already declined / accepted
						has_country_flag = NEP_accepted_himalayan_defense_pact # If they are independent at game start then they get invited already. Put this here to prevent this decision from popping up if they already declined / accepted
					}
				}
			}
		}

		ai_will_do = { base = 1 }

		complete_effect = {
			SKK = {
				country_event = {
					id = nepal.8
					days = 2
					hours = 12
				}
			}
			custom_effect_tooltip = generic_skip_one_line_tt
			custom_effect_tooltip = NEP_they_are_more_likely_to_accept_if
		}
	}

	NEP_invite_ladakh_into_defensive_pact_r56 = {
		icon = GFX_decision_eng_trade_unions_support
		targets = { KAS }
		fire_only_once = yes
		priority = 25

		available = {
			has_war = no
			is_subject = no
			TIB = { has_country_flag = NEP_accepted_himalayan_defense_pact } # We don't share borders, duh
			if = {
				limit = { is_faction_leader = yes }
				KAS = {
					exists = yes
					is_subject = no
					has_war = no
					is_in_faction = no
					if = { # So that we don't invite enemy ideology
						limit = { ROOT = { has_government = communism } }
						OR = {
							has_government = communism
							has_government = neutrality
						}

						else_if = {
							limit = { ROOT = { has_government = democratic } }
							OR = {
								has_government = democratic
								has_government = neutrality
							}
						}

						else_if = {
							limit = { ROOT = { has_government = fascism } }
							OR = {
								has_government = fascism
								has_government = neutrality
							}
						}
					}
				}
				else = {
					KAS = {
						exists = yes
						is_subject = no
						has_war = no
					}
					if = { # So that we don't invite enemy ideology
						limit = { ROOT = { has_government = communism } }
						OR = {
							has_government = communism
							has_government = neutrality
						}

						else_if = {
							limit = { ROOT = { has_government = democratic } }
							OR = {
								has_government = democratic
								has_government = neutrality
							}
						}

						else_if = {
							limit = { ROOT = { has_government = fascism } }
							OR = {
								has_government = fascism
								has_government = neutrality
							}
						}
					}
				}
			}
		}

		visible = {
			has_country_flag = NEP_we_want_friendship # So that this doesn't appear if Nepal decides to go expansionist
			KAS = {
				exists = yes
				NOT = {
					is_subject_of = ROOT
					is_in_faction_with = ROOT
				}
			}
		}

		ai_will_do = { base = 1 }

		complete_effect = {
			KAS = {
				country_event = {
					id = nepal.8
					days = 2
					hours = 12
				}
			}
			custom_effect_tooltip = generic_skip_one_line_tt
			custom_effect_tooltip = NEP_they_are_more_likely_to_accept_if
		}
	}

	NEP_invade_upper_mustang_r56 = {
		icon = GFX_decision_generic_tank
		fire_only_once = yes
		priority = 5

		cost = 10
		days_remove = 7

		available = {
			323 = {
				is_demilitarized_zone = no
				is_fully_controlled_by = ROOT
			}
			divisions_in_state = {
				size > 0
				state = 323
			}
		}

		visible = { has_completed_focus = NEP_our_role_in_himalayan_politics_r56 }

		ai_will_do = { 
			base = 0
			modifier = {
				add = 1
				has_government = fascism
			}

			modifier = {
				add = 1
				has_government = communism
			}

			modifier = {
				add = 1
				is_major = yes
			}
		}

		modifier = { political_power_factor = -0.1 }

		remove_effect = {
			add_stability = -0.02
			323 = { add_extra_state_shared_building_slots = 1 }
			custom_effect_tooltip = generic_skip_one_line_tt
			if = {
				limit = {
					TIB = {
						exists = yes
						is_subject = no
						NOT = {
							OR = {
								has_war_with = ROOT
								is_ally_with = ROOT
							}
						}
					}
				}
				TIB = { country_event = mustang_crisis.1 }
			}
		}
	}
}

political_actions = {
	NEP_border_war_with_tibet_r56 = {
		icon = GFX_decision_hol_draw_up_staff_plans
		allowed = {
			OR = {
				original_tag = NEP
				original_tag = TIB
			} 
		}

		priority = 50
		days_mission_timeout = 30
		fire_only_once = yes
		is_good = no

		available = { hidden_trigger = { always = no } }
		activation = { has_country_flag = NEP_its_going_down }

		timeout_effect = {
			start_border_war = {
				change_state_after_war = no
				attacker = {
					state = 757
					num_provinces = 10
					on_win = mustang_crisis.4
					on_lose = mustang_crisis.5
					on_cancel = mustang_crisis.6
				}
				defender = {
					state = 323
					num_provinces = 7
					on_win = mustang_crisis.4
					on_lose = mustang_crisis.5
					on_cancel = mustang_crisis.6
				}
			}
		}
	}
}